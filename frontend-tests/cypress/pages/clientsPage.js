/// <reference types="cypress" />

//elements

// functions

function createNewClient(cy) {
    
}

function deleteClient(cy) {
    
}

// exports

module.exports = {
    createNewClient,
    deleteClient
}